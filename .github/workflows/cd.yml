name: env variables
on: push
env:
    WF_ENV: avaliable to all jobs

jobs:
    log-env:
        runs-on: ubuntu-latest
        env:
                    JOB_ENV: avaliable to all jobs
        steps:
            - name: test
              run: | 
                echo $JOB_ENV